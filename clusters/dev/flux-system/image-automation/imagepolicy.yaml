apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: fastapi-app
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: fastapi-app
  filterTags:
    pattern: '^main-.*'
  policy:
    alphabetical:
      order: desc
